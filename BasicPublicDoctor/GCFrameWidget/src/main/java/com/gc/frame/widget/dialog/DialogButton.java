package com.gc.frame.widget.dialog;

/**
 * Dialog Button's ID Class
 * 
 * @author Xu
 * 
 */
public class DialogButton {
	/**
	 * left button's id (if dialog has only one button) /
	 */
	public static final int LEFT = 0;
	/**
	 * right button's id (if dialog has two buttons) /
	 */
	public static final int RIGHT = 1;
	/**
	 * middle button's id (if dialog has three buttons) /
	 */
	public static final int MIDDLE = 2;
}
